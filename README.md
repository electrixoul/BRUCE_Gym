# BRUCE_Gazebointerface

To install:
cd into Gazebointerface/build/ then
```bash```
sudo cmake --install .
```

To uninstall:
cd into Gazebointerface/build/ then
```bash```
xargs rm < install_manifest.txt
```
